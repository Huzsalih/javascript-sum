function computeSum(a, b) {
    if (a === b) {
      return 3 * (a + b);
    }
    return a + b;
  }
  
  // Example usage:
  console.log(computeSum(5, 5)); // Output: 30
  console.log(computeSum(5, 3)); // Output: 8
  